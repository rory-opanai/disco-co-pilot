{
  "name": "disco-co-pilot",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "build": "npm run build --workspace frontend",
    "start": "npm run start --workspace frontend",
    "db:seed": "NODE_OPTIONS=--no-warnings npx tsx db/seed/seed_playbooks.ts"
  },
  "devDependencies": {}
}
